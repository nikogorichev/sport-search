// import { ADD_PARTICIPANT, DELETE_PARTICIPANT } from '../actionTypes/participantAT';

// const initialState = { participants: [] }

// export const participantsReducer = (state = initialState, action) => {
//   switch (action.type) {

//     case ADD_PARTICIPANT:
//       console.log('fact1', state)

//       return { ...state, participants: [...state.partisipants, action.payload] }
    
//     case DELETE_PARTICIPANT:
//       console.log('fact', state)
//       return {
//         ...state,
//         // participants: state.participants.filter((el) => el.user_id !== action.payload.user_id && el.EventId !== action.payload.event_id)
//       }

//     default:
//       return state
//   }
// }
